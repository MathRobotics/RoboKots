"""Local and whole-body outward computation kernels.

Import the required module explicitly. JAX and soft-link implementations are
loaded only when requested. State storage and public
derivative dispatch live in the parent outward package, not in these kernels.
"""
