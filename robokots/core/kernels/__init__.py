"""Shared local and whole-body robotics computation kernels.

Import the required module explicitly. JAX and soft-link implementations are
loaded only when requested. Concrete state storage, computation orchestration,
and public derivative dispatch remain outside core.
"""
